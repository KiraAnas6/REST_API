# Flask REST API

A simple REST API built using **Flask**, **Flask-RESTful**, and **SQLAlchemy ORM**.  
The API supports basic CRUD operations (**GET, POST, PATCH, DELETE**) and returns JSON responses.

---

## 🚀 Features

- REST API using **Flask**
- Database handled with **SQLAlchemy ORM**
- Organized with **Api & Resource**
- Input parsing using **reqparse**
- Response formatting with **fields**
- Error handling using **abort**
- Supports CRUD operations on user data

---

## 📦 Technologies & Packages

| Library | Usage |
|--------|--------|
| Flask | Main framework |
| Flask-RESTful (Api, Resource, reqparse, fields, abort) | API routing & request handling |
| SQLAlchemy | ORM for database models |
| Python | Core language |

---
